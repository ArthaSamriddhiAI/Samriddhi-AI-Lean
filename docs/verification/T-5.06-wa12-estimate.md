# T-5.06 Phase 5 S1 smoke test — WA12 API-call estimate

> WA12 gate. This is the first Anthropic API spend in the T-5.06 workstream. No LLM call fires until the planning chat approves this estimate. The deterministic work (Phases 1–4: math, schema, S1 wiring, 45-case verification) used zero API.

## What fires

The smoke test validates **one thing**: that S1 diagnostic-mode, given a `StitchedContext` that now carries `time_series_performance` (ADR-0029, Option II), cites time-series facts in its narrative, with snapshot IDs, and surfaces sentinels honestly.

**Recommended approach — targeted S1-only runs (1 LLM call per case).** Build a real `StitchedContext` deterministically (`computeMetrics` + `route` + `runTimeSeriesPerformanceDeterministic`, all zero-API) for the chosen case, `stitch(...)` it with the time-series output, and call `runS1Diagnostic`. This fires exactly **one** `claude-opus-4-7` call per case and needs no DB seeding. It tests the integration directly.

3 cases (per the ping's t8 / t5 / t0 suggestion), one archetype to isolate the snapshot effect:

| Case | Snapshot | Why |
|---|---|---|
| Bhatt @ t8_q2_2028 | latest, all data present | rich evaluable equity + evolution → tests citation of windows + evolution |
| Bhatt @ t5_q3_2027 | bank-shock stress regime | negative/stress short-window returns → tests honest stress narrative |
| Bhatt @ t0_q2_2026 | baseline, no prior snapshot | tests S1 does NOT claim evolution (`no_prior_snapshot_available`) |

(Bhatt chosen for evaluable equity to cite + 4 PMS / 1 AIF sentinels + the four-thesis notice. Planning chat may swap archetypes.)

## Per-run token estimate

S1 = `claude-opus-4-7`, `max_tokens: 3500`.

| Component | Tokens (est.) |
|---|---|
| System prompt (`s1_diagnostic_mode.md` skill) | ~2,500 |
| User prompt: case header + instructions | ~1,500 |
| User prompt: stitched context JSON (metrics + **time_series_performance** for ~12 holdings × 6 windows × 2 + sleeves/portfolio/evolution + null evidence + pre_observations + router) | ~8,000 |
| Holdings appendix | ~500 |
| **Input per run** | **~12,500** |
| **Output per run** (briefing JSON, 7 sections; capped at 3,500) | **~3,000** |

## Dollar estimate

Assumed `claude-opus-4-7` rates (Opus tier): **$15 / M input, $75 / M output**. (Flag: substitute the actual contracted rate if different; the order of magnitude holds.)

| | Input | Output | Per run |
|---|---|---|---|
| Tokens | 12,500 | 3,000 | |
| Cost | $0.19 | $0.23 | **~$0.42** |

- **3 runs: ~$1.25.**
- Retry buffer (the harness retries on schema-validation failure; assume up to one extra attempt across the 3): **~+$0.40**.
- **Total estimate: ~$1.25–$1.65** (≈ 37.5k input + 9k output tokens, plus retry buffer).

## Alternative (not recommended): full end-to-end pipeline

A full `runDiagnosticPipeline` per case fires E1, E2, E3, E4, E6, E7, S1, and A2 (~8 LLM calls) and requires the DB seeded with the archetype investors. Estimated **~$4–6 per case → ~$12–18 for 3**, and it tests far more than the S1↔time-series integration. Not proportional to Phase 5's "light" intent. Available if the planning chat wants a true end-to-end exercise.

## Gate

**No LLM call fires until approved.** On approval I will: load the API key from `.env.local`, run the 3 targeted S1 cases, capture each S1 narrative + the time-series output it was given, validate that S1 cites time-series facts with snapshot IDs and surfaces sentinels honestly (especially the t0 case must NOT claim evolution), and append the results to `docs/verification/T-5.06-verification.md` (commit 30).
