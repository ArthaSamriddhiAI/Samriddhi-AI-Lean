# Tech debt log

T-series. Things in the codebase that work but are not production-grade, and technical debt about the engineering / audit-trail process itself. Restructured out of the combined `PRODUCT_DEBT_LOG.md`; see `README.md` for the convention.

| ID | Description | Severity | Originating workstream | Target fix workstream |
|----|-------------|----------|------------------------|------------------------|
| T1 | `STUB_MODE` default flipped to `false` post-M0 (live mode is the working default now that tier-2 access and budget exist). The runtime toggle (Settings: inherit env / force on / force off) is preserved for offline demos. Open question: whether `STUB_MODE` should become a deploy-time feature flag or stay env-driven. | Low | Post-M0 housekeeping (2026-05-17) | Slice 9 deployment (deploy-time flag decision); reviewed in the Slice 7 audit |
| T2 | Pre-M0 hardcoded MVP SEBI regulatory rules table in G2. Resolved 2026-05-17: G2 now grounds its minimum-ticket reference data in `sebi_boundaries` via M0.IndianContext (verdicts unchanged, now sebi_001-cited). Retained here for provenance. | Medium (resolved) | Slice 3 (MVP rules table) | M0.IndianContext integration (done, PR #1) |
| T3 | No test harnesses outside M0.IndianContext. M0 ships verify scripts (`scripts/_verify-indian-context.ts`, `_verify-governance-regrounding.ts`, `_verify-stub-replay-sharma.ts`); the pipeline, governance gates, IC1 orchestrator, and PDF renderers have none. **Partially stale post-A2 Slice 4.6a (2026-05-18):** deterministic test harnesses now exist for A2 Layer 1 (`_verify-a2-classification.ts`, 11 tests) and for materiality (`_verify-materiality.ts`); `portfolio-risk-analytics.ts`, `router.ts`, and `stitcher.ts` remain uncovered (now tracked specifically as T10/T11/T12), and the unified-runner question (T5) remains open. Original framing preserved for provenance. | Medium | M0.IndianContext (surfaced the gap by contrast) | Slice 7 polish; per-module verify scripts as each area is next touched |
| T4 | Single-tenant assumption throughout the codebase. No firm-scoping on cases, investors, or settings; multi-firm tenancy is post-MVP. | Medium | Slice 1 scaffolding | Post-MVP tenancy workstream |
| T5 | No automated CI. Typecheck and verify scripts run manually; nothing gates a commit or a merge. **Updated post-A2 Slice 4.6a (2026-05-18):** A2's `_verify-*` pattern (now seven scripts plus a `scripts/README.md` prefix legend) is the established precedent for deterministic verification; T5's eventual resolution (a unified runner / CI) should design against this convention rather than green-fielding. Original framing preserved for provenance. | Medium | Slice 1 scaffolding | Slice 7 polish or a dedicated CI workstream |
| T6 | `.env` / `.env.local` carry configuration (API keys, runtime toggles) that should eventually move to a config management layer (Doppler, Vault, or similar) rather than living in dotfiles. | Medium | Slice 1 scaffolding | Slice 9 deployment |
| T7 | RAG scoping. RAG was evaluated for M0.IndianContext and rejected (deterministic discipline over a curated YAML store is the right shape for an auditability-first platform). RAG is still worth scoping for other use cases; see the prose note below. | Low | M0.IndianContext (evaluation) | Slice 7 audit produces a RAG roadmap entry |
| T8 | Audit file structure: per-workstream audits vs cumulative conventions. The A2 audit file (`docs/audits/2026-05-18_a2_classification.md`) carries both the workstream-specific audit body (findings, design, reconciliations, calibration) and a forward-looking Hand-off section that propagates conventions (working agreement 10, ships-narrow/captures-wide discipline) to the next capability workstream. These are different artifacts in one file. Should per-workstream audit files stay strictly workstream-scoped, with cumulative conventions in a separate durable file (e.g. `docs/conventions.md`)? Do NOT refactor the A2 audit file in this workstream; decide later with several workstreams' content to design against. See detailed framing below the table. (Partially mitigated by working agreement 11's dual-write to `docs/workstreams/`, but the audit-vs-conventions split question stands.) **RESOLVED 2026-05-19 (risk-reward workstream): the per-WA file structure in `docs/working_agreements/` (one file per agreement, with rationale/trigger/examples/cross-references) is the durable conventions home. The deferred single `docs/conventions.md` is superseded and no longer a separate task; `a2_classification_handoff.md` now points to the canonical location rather than carrying WA text inline. The audit-vs-conventions split is closed: conventions live in `docs/working_agreements/`, per-workstream audits stay workstream-scoped in `docs/audits/`.** | Low (resolved 2026-05-19) | A2 classification (Slice 4.6a) Step 4 round | Resolved by risk-reward WA per-file migration |
| T9 | Layer 2 fact-grounding verification. Layer 1 is deterministic and replayable by construction; Layer 2 (reason text) is not. The only grounding check today is a human spot-check at Step 5 review, which does not scale. Should there be a deterministic post-Layer-2 check that cited specifics (return figures, threshold numbers, fund names, holdings counts) are traceable to the structured inputs Layer 1 consumed? See detailed framing below the table. | Medium | A2 classification (Slice 4.6a) Step 5 review (the 6.86% investigation) | Slice 7 technical debt audit pass |
| T10 | No deterministic test coverage for `lib/agents/portfolio-risk-analytics.ts` (`computeMetrics`). A2 Layer 1 anchors to this module's output (position/sector/wrapper flags, HHI tiers, liquidity buckets); an undetected bug here is inherited by A2 silently. Highest-value of the three coverage gaps. A test should cover the four threshold boundaries under the ADR 0005 convention, wrapper-count logic, HHI ceiling tiers, and liquidity-bucket categorisation, modelled on the A2 Layer 1 verify shape. | Medium | A2 classification (Slice 4.6a) PR #3 test-discipline audit | A coverage pass on the M0 deterministic modules (Slice 7 or when next touched) |
| T11 | No deterministic test coverage for `lib/agents/router.ts` (`route` / `routeProposedAction`). The activation logic decides which LLM agents fire and is currently inferred from prose in `m0_router.md` with no test. A test should cover diagnostic vs proposed_action paths, each E-agent applicability condition, and edge cases (empty / single-asset portfolio). | Medium | A2 classification (Slice 4.6a) PR #3 test-discipline audit | A coverage pass on the M0 deterministic modules (Slice 7 or when next touched) |
| T12 | No deterministic test coverage for `lib/agents/stitcher.ts` (`stitch`). Lower urgency than T10/T11 because stitcher failures are partly caught by S1 schema validation, but logged for completeness so the three-module coverage gap is a coherent tracked set rather than selectively recorded. A test should cover StitchedContext shape, pre-observation derivation from metric thresholds, and the no-invention discipline. | Low | A2 classification (Slice 4.6a) PR #3 test-discipline audit | A coverage pass on the M0 deterministic modules (Slice 7 or when next touched) |
| T13 | Snapshot storage format. Enriched snapshot JSONs are roughly 13MB each (about 120MB across the 9-snapshot suite), grown further by the risk-reward NAV regeneration. A more efficient format (Parquet, MessagePack, or per-instrument splitting) would cut load time and repo size. Accepted dev-phase tech debt; the LRU loader keeps runtime cost bounded. | Low | Risk-reward (Step 3a regeneration) | Slice 7 or a dedicated storage-format pass |
| T14 | E2 (industry / business-model agent) may supplement from training-data industry knowledge on uncovered cases even when the scope-builder's no-supplementation guardrail (ADR-0024, DP2) instructs otherwise. The guardrail reliably stops invented fund sector-WEIGHTS (Iyengar and Malhotra E2 correctly state "sector data not available"), but for a strongly thematic PMS target (Bhatt's Ambit Build India, infrastructure) E2 brings model-knowledge INDUSTRY context (e.g., Union Budget FY26 infra capex Rs 11.11 lakh crore, L&T order book) because its skill is directive about industry analysis. Accepted for this batch per the pre-agreed disposition: these are plausible, labeled, non-contradicting (moderate, not the E7-style snapshot contradiction) and do not drive the verdict. Future work: tighten the E2 skill file's industry-inference behavior, or expand snapshot sector/industry coverage so E2 grounds rather than supplements. | Low | s1-case-generation (case: bhatt) | E2 skill revision or snapshot sector/industry coverage expansion |
| T15 | Deterministic verify scripts that assert pipeline behavior should encode per-target-category (or per-relevant-dimension) expectations rather than blanket assertions. Surfaced during Phase D when `_verify-governance-regrounding`'s blanket "G2 cites sebi_001" assertion failed on MF target cases (legitimately, per P25 behavior). The script was updated in the same commit; this debt entry exists to remind future verify-script authors that the per-target-category pattern is the right shape, not blanket assertions that assume one product class. Unblocking-fix: new verify scripts adopt per-target-category patterns where pipeline behavior depends on target shape; existing verify scripts updated incrementally as their assumptions are surfaced. Cross-references: P25 (G2 MF scheme rule curation). | Low (no functional impact; documentation and forward-guidance) | snapshot-data-extraction Phase D (case-batch rebase surfaced the script's outdated assumption) | Future verify-script authoring as needed; no dedicated workstream required |
| T16 | E5 inert dispatch. In Samriddhi 2 diagnostic, the router can set `e5 = true` on a case with unlisted holdings, but the pipeline never dispatches E5 and the `EvidenceBundle` has no `e5` slot, so `evidence.e5` is always null. The router asserts an activation the pipeline does not honour; either wire E5 diagnostic or have the router stop asserting it. | Medium | v11.3 architecture mapping | TBD |
| T17 | Skill-vs-implementation schema divergence. Across IC1 (Chair, Minutes Recorder), S1 (diagnostic mode), and A1, the skill `.md` files describe richer authored output schemas than the TypeScript renderers actually emit today (A1's approval-recommendation enum, Minutes Recorder's cryptographic audit trail, Chair's outcome/confidence fields, S1-diagnostic's health-verdict schema). Either the renderers should be expanded to ship the authored shape or the skills should be trimmed to match what runs. | Medium | v11.3 architecture mapping | TBD |
| T18 | Trailing-window return computation runs at agent runtime, not at snapshot enrichment. Self-contained for MVP; in production, the choice between agent-runtime (firm-portable) and enrichment-time (performant) becomes a real-firm-deployment decision. | Medium | T-5.06 (time-series-performance) | TBD |
| T19 | Cross-snapshot evolution in time-series-performance compares t_n vs t_{n-1} only. Configurable reference points (e.g. "since the bank shock at t5") deferred. The pair-aware loader (`loadSnapshotPair`) already supports an arbitrary referenceId; the deferral is in the pipeline-level resolution (`priorSnapshotId` is hardcoded to immediately-prior). | Low | T-5.06 (time-series-performance) | TBD |
| T20 | Model-tiering hypothesis (T-5.14, 2026-06-01). The per-agent model map (`lib/agents/skill-loader.ts`) was tiered to cognitive demand: e6 (PMS/AIF, the least-structured, most judgment-heavy evidence) and `s1` (the diagnostic synthesis agent) upgraded to Opus 4.8; a2's Layer 2 reason-text gloss dropped to Haiku 4.5; a3 (so-what advisor action) moved to Opus 4.8; e1-e5 and e7 kept on Sonnet 4.6. Two assignments are untested hypotheses the primary chose without budget to A/B them: (a) e6-on-Opus while e1-e5/e7 stay Sonnet rests on the hypothesis that the hardest, least-structured evidence work benefits most from the premium model; if re-validation shows the evidence layer reads uneven across agents, this split is the conscious lever to revisit. (b) `s1` upgraded to Opus 4.8 now, to be eyeballed at re-validation and reverted to Sonnet if it reads no better. RESOLVED 2026-06-02 (real-t0 re-fire eyeball): s1 reads as layered diagnostic synthesis (connecting allocation, behavioural pattern, and stated-vs-revealed risk appetite into a verdict) and e6 as sophisticated, honestly-calibrated PMS/AIF evidence (domain-aware on quality-style rotation and HWM/hurdle fee classes while capping confidence where the snapshot has no record); both Opus 4.8 assignments are kept-justified and the e6-vs-rest split read even. The a2-to-Haiku (mechanical gloss of deterministic Layer 1 verdicts) and a3-to-Opus-4.8 (free, strictly better upgrade from 4.7) moves are low-risk. Note: products are Samriddhi 1 and Samriddhi 2; `s1` is the agent. | Low | T-5.14 model tiering (primary's call, WA28) | Re-validation eyeball of the re-fired Samriddhi 2 cases (e6-vs-rest evenness; s1 quality) |
| T21 | Cash-metric basis adjacency (T-5.14 pre-merge audit, 2026-06-02). `cashDeployment.deploymentGapPct` (`lib/agents/portfolio-risk-analytics.ts:428`) is computed against the flat universal `MODEL_BANDS.Cash.max` (5%; `lib/agents/stitcher.ts` labels it `model_cash_ceiling_pct: 5`), while the allocation table's `assetClass.Cash` is threaded to the per-investor mandate (`Cash.targetPct` 6% for menon; the Finding-5 per-investor bands). So the two cash metrics use different bases: menon's deployment gap is 86.6 minus 5 = 81.6pp (versus the universal ceiling) while its allocation deviation is 86.6 minus 6 = 80.6pp (versus the mandate target). Both are correct and computed; they only read as non-reconciling when surfaced adjacently, which the audit caught in menon's prose. Pre-existing (the real-data work did not introduce it); surfaced because menon is the only high-cash case. Mitigated in the prose by making the deployment-gap basis explicit (`above the 5% model deployment ceiling`); the underlying remedy (point `cashDeployment` at the per-investor mandate, or otherwise reconcile the two bases) is a separate future deterministic patch that would change the computed `deploymentGapPct` and stale menon's `81.6pp` prose, so it is deliberately NOT bundled into the T-5.14 PR. | Low | T-5.14 pre-merge audit (pre-existing Finding-5 adjacency) | A future deterministic metrics patch (reconcile cashDeployment's band source to the per-investor mandate) |

**RAG scoping note (T7).** RAG implementation was assessed for M0.IndianContext and deliberately rejected: the platform's USP is deterministic, auditable, citation-grounded reasoning, and a curated YAML knowledge store with explicit citations serves that better than retrieval over an opaque index. RAG remains worth scoping for use cases where the corpus is large and the auditability bar is different:

- **Slice 6 read-only Q&A chat:** single-case scope. Low RAG benefit; the frozen case content is small and already structured, so direct context is sufficient.
- **Cross-case search:** high RAG benefit. Querying across many cases is a genuine retrieval problem.
- **Historical case archive search at firm scale:** highest RAG benefit. Large, growing corpus where retrieval quality dominates.

The Slice 7 product debt audit should turn this into an explicit RAG roadmap entry (which use cases, in what order, with what guardrails to preserve the auditability posture).

**T8 detail (structure-of-the-audit-system question).** Surfaced by the A2 Slice 4.6a Step 4 round. The audit file `docs/audits/2026-05-18_a2_classification.md` ended up carrying both the workstream-specific audit body (Step 0 findings, design proposal, reconciliations, alternatives, calibration validations: the technical record of this workstream) and a forward-looking Hand-off section meant to propagate conventions (working agreement 10, the ships-narrow/captures-wide discipline) to the next capability workstream. These are different artifacts in a single file. The question: should each workstream's audit file remain strictly workstream-scoped (technical record of what was built and why), with cumulative conventions and hand-off notes living in a separate durable file (for example `docs/conventions.md` or `docs/working_agreements.md`) that workstreams update on commit and inherit on creation? Routing: this is a structure-of-the-audit-system question, evaluated deliberately once enough workstreams have shipped to know which shape serves future maintenance better; three or four capability workstreams of content is probably enough. Do not refactor the current A2 audit file in this workstream; the split, if any, happens later with the benefit of multiple workstreams' content to design against. Why this is T-series and not P-series or D-series: T-series is technical debt about the engineering or audit-trail process itself, distinct from P-series (product-stance questions about what Samriddhi does) and D-series (data-quality issues in fixtures). T-series entries surface when the way we build starts to need attention separately from what we build.

**T9 detail (Layer 2 fact-grounding verification).** Surfaced by the A2 Slice 4.6a Step 5 review. The recurring "5Y return 6.86%" across three different fund references in the reason text was investigated against the source data snapshot and confirmed as genuine coincidence: Axis Large Cap Fund 5Y = 0.068625 (snapshot `mf_funds.amfi_code=112277`; "Axis Bluechip" is the old name, renamed 2024); Kotak Global Emerging Market Overseas Equity Omni FOF 5Y = 0.068588 (snapshot `amfi_code=106441`; the D6-mislabelled "Kotak Emerging Equity" maps here). Both round to 6.86% at two decimal places. Layer 2 is correctly sourcing per-fund data in this instance; the recurrence is the data telling the truth, not fabrication. The general question the investigation surfaced: how does future audit work confirm Layer 2 is grounded in source data rather than fabricating or mis-attributing? Layer 1 is deterministic and replayable; Layer 2 is not. The current discipline (human spot-check at Step 5) is the only grounding check and does not scale beyond a handful of cases or suspect-looking patterns. Options for the Slice 7 technical debt audit to evaluate: (a) a number-grounding validator that parses Layer 2 reason text for numeric claims and confirms each appears in the structured input bundle; (b) a structured-reasons-plus-gloss split where Layer 2 returns structured driver records with explicit input-source references and a separate pass produces the prose, so the structured layer is verifiable independently of the prose; (c) accept the current discipline as adequate for the diagnostic-only MVP and revisit when A2 verdicts feed proposal-side flows. Routing: a Layer 2 architecture question, not an A2-specific implementation question; deferred to the Slice 7 technical debt audit pass. This entry is the durable record that the 6.86% investigation happened, what it found, and the general question it raised, so a future audit does not have to re-derive the forensic work.

**T16 update, 2026-05, from T-5.07/T-5.08 workstream:** The Samriddhi 2 Analyst
Reports port (T-5.08) considered rendering an E5 row with a "pending
implementation" sentinel and rejected the option on WA15 grounds (a UI element
without capability behind it is a wireframe-without-capability inversion). The
ported component therefore does not render an E5 row at all; the adapter from
the Samriddhi 2 evidence bundle to the rendered surface omits E5 entirely. E5
implementation remains deferred to a future workstream (likely Package 6 or
later). When the implementation lands, it touches: a new
`lib/agents/e5-unlisted-equity.ts` sibling, an `e5` slot in `EvidenceBundle`,
an enqueue branch in `runDiagnosticPipeline`, fixture injection across
affected cases, and the T-5.08 adapter's E5 emission path. See ADR-0030 for
the don't-render decision context.

**T17 v14 sharpening (2026-05-27).** The planner v14 scopes "Samriddhi 2 A1 challenge" (provisional task ID T-5.13, not yet landed) under "Decision 2A": adopt the existing flat `A1Output = { challenges: AdvisoryChallengeItem[] }` shape on the diagnostic firing path; do NOT expand to the richer `approval_recommendation` enum (proceed / proceed_with_conditions / defer / escalate_to_compliance), `confidence` float, and categorized concern arrays that the skill file describes. Rationale: the richer schema was authored against Samriddhi 1 proposed-action semantics; it has no clean fit on a diagnostic case that has no action to approve. T17 reconciliation for the richer schema therefore stays a Samriddhi 1 workstream and does not gate the diagnostic firing path. CC grounding ping (read-only, 2026-05-27) confirmed `A1Output` in `lib/agents/case/a1-case.ts` is the flat shape today.

**T18 detail (deferred production decision).** Trailing-window return computation (1M / 3M / 6M / 1Y / 3Y / SI) runs at agent invocation rather than at snapshot enrichment.

Two future-state considerations:

(a) **Performance:** if agent invocation latency matters in production, lift `computeTrailingWindowReturns()` into the enrichment pipeline.

(b) **Firm portability:** in a real wealth advisory deployment, snapshot enrichment may be firm-specific. Different custodians produce different field shapes feeding what will become a "data management layer" that normalizes to canonical schema. Agent-runtime computation is the more portable choice because it only assumes `monthly_nav` and `monthly_prices` are present in canonical shape, not that the firm pre-computed return stats.

**Resolution:** the choice of (a) vs (b) is a production-deployment decision, not a code-debt decision. Recommend supporting both: enrichment can pre-compute and the agent can fall back to runtime if pre-computed fields are absent.

See ADR-0028 production deployment considerations section. See P31 (product debt log) for the product-debt facing.

