# Data debt log

D-series. Fixture and reference-data quality issues ("the data is wrong"), routed to fixture refresh. Restructured out of the combined `PRODUCT_DEBT_LOG.md`; see `README.md` for the convention.

| ID | Description | Severity | Originating workstream | Target fix workstream |
|----|-------------|----------|------------------------|------------------------|
| D1 | 8 of 107 curated YAML entries are marked `confidence: indicative` (e.g., pmlt_001, dem_012). A future audit should upgrade these to authoritative where a citable source exists. | Medium | M0.IndianContext (Workstream C curation) | Workstream C follow-up or the Slice 7 audit |
| D2 | Synthetic investor profiles (A1-A5, A6-A14) stand in for real onboarded clients. Adequate for the demo; production credibility wants real or realistic onboarded data. | Medium | Slice 1 fixtures | Post-MVP, gated on real client onboarding |
| D3 | No eCAS parsing pipeline. Investor holdings fixtures are created manually; there is no ingestion path from a real eCAS statement. | Medium | Slice 1 fixtures | Post-MVP ingestion workstream |
| D4 | No `regulatory_changelog` automation. Regulatory updates require manual YAML edits; nothing watches sources or proposes diffs. | Low | M0.IndianContext (Workstream C) | Post-MVP, low priority until the store changes often |
| D5 | Sharma's IC1 stubs were re-recorded post-M0 (grounded in IndianContext); stubs for other cases predate M0. If `STUB_MODE` is ever used as the primary mode again, the non-Sharma stubs should be re-recorded so replay matches the post-M0 reasoning. | Low | M0.IndianContext (re-recorded Sharma only) | The workstream that next needs STUB_MODE as primary |
| D6 | "Kotak Emerging Equity Fund" appears in the Malhotra, Surana, and Sharma-S2 holdings labelled as a domestic mid-cap fund; the underlying fund is actually an overseas Fund-of-Funds. E7 flags this as a `negative` thesis (structure does not match labelled category) and A2 faithfully propagates the E7 verdict to Review on the affected holdings. A2's behaviour is correct given the input; the underlying issue is that the snapshot fixture data carries the wrong category label on this fund. Fix in the next fixture refresh (snapshot regeneration or manual correction of the affected investor holding records). Not an A2 issue and not a product-stance question. (Numbered D6: D1-D5 were already in use by the M0.IndianContext / Slice 1 data-debt entries; the Step 4 prompt's "D1" label is renumbered to avoid collision.) | Medium | A2 classification (Slice 4.6a) Step 4 dry-run | Next fixture refresh |
| D7 | The frozen S2 case fixtures contain long-dash glyphs in the pre-existing S1 briefing and evidence prose (e.g. 102 lines in `c-2026-05-14-bhatt-01.json` at the pre-A2 commit), generated by the Slice 2/3 pipeline before the no-long-dash discipline (working agreement 7) existed. The A2 backfill is purely additive (verified: each fixture is N added / 1 removed, zero added lines contain a long dash) and does not introduce, modify, or re-author this prose; it appears only as unchanged context. Rewriting frozen briefings is out of scope for the A2 capability workstream (working agreements 5 and 9; it would churn expensive frozen content, the exact outcome the additive-backfill design avoided). Resolve in the next fixture refresh, or by a one-off sanitiser pass over the persisted briefing/evidence prose, owned by whichever workstream next regenerates or refreshes the S2 fixtures. Not an A2 defect. | Low | A2 classification (Slice 4.6a) Step 4 fixture-write verification | Next fixture refresh or a dedicated fixture-prose sanitisation pass |
| D8 | Cluster-3 source-data issues beyond D6, referenced (not absorbed) by risk-reward via existing degradation paths. (a) Axis Bluechip Fund and HDFC Index Fund Nifty 50 Plan are absent from source `mf_funds[]`. (b) About 24 stocks have broken `market_cap_rs_cr` (5 above 1.5M Cr, about 19 at zero) and default to small cap-tier, so their `tier_b_stats._meta.benchmark_index_id` is wrong by construction. (c) SBI escaped the bank-shock substring detection (source t5 +10% instead of the designed -18%); risk-reward uses sector aggregates via `_meta.sector` rather than name detection, and excludes SBI from regime-validation assertions. | Medium | Risk-reward (Step 1 audit) | Cluster-3 source-data cleanup pass |
| D9 | Snapshot `aif` block carries asymmetric coverage across AIF categories: Cat I (venture, angel, stressed-asset) is reasonably represented in the "E6 Agent Input Ready" list, but Cat II (especially private credit) is largely absent. Surfaced during the Samriddhi 1 case batch (case: menon, a Cat II private credit AIF). Without coverage E6 cannot ground on Cat II private-credit proposals; the honest-miss plus guardrail (ADR-0026) correctly state "not in coverage" rather than hallucinating manager/fee/capacity figures. Unblocking fix: either (a) expand the `aif` block's "E6 Agent Input Ready" list to include Cat II private-credit funds with the same field shape (`management_fee_pct`, `performance_fee_pct`, `hurdle_rate_pct`, `minimum_investment_cr`, `fund_manager_names`, `redemption_terms`), or (b) accept the limitation and scope future Samriddhi 1 cases away from Cat II private-credit targets. Cross-references ADR-0024, ADR-0026, the menon case fixture, and the foundation AIF look-through scope-out language. | Medium | s1-case-generation (case: menon) | Snapshot coverage expansion for Cat II AIF (data-sourcing decision; may need BD/partnership given Cat II disclosure opacity) |
| D10 | Snapshot extraction produces prose-polluted top-5 holding names on a subset of MFs in the ~233-with-top-5-disclosure set (snapshot `t0_q2_2026`). Surfaced during T-5.07 verification design: several funds' "Top 5 Holdings (JSON)" carry literal prose adjacent to the holding name, e.g. Nippon India Vision Fund's top-5 contains `"Current Investment Philosophy ... ICICI Bank Limited"` and `"Healthcare Services ... HDFC Bank Limited"` — PDF/HTML-scraping artifacts where descriptive text was included in the holding string. **Impact on T-5.07 portfolio-overlap:** the agent's stock-name normalizer strips punctuation and corporate suffixes (Ltd, Limited, Co, Corp) but deliberately does NOT clean prose pollution, so polluted funds' holdings will not match clean ones and Layer 1 (stock-level overlap) under-counts overlap for any pair involving a polluted fund. **Why not fix in the agent:** a more aggressive normalizer would over-merge legitimate names (false-positive overlap is worse than an honest under-count); stock matching is fundamentally exact-match with controlled normalization. **Right fix is upstream:** the extraction pipeline should trim known prose patterns before the snapshot row lands. **Scope:** affected funds need a sweep across the ~233-set to enumerate; user-visible through T-5.08's Analyst Reports (affected funds systematically show under-counted overlap). The T-5.07 verification deliberately picked clean funds (ABSL ELSS Tax Saver × Focused) for the stock-level case to avoid this. | Medium | T-5.07/T-5.08 workstream (verification design) | Snapshot extraction pipeline prose-trim pass (owner of extraction) |
| D11 | Value Research scrape at agent runtime for MF star ratings (per T-5.15) is fragile by construction: Value Research's HTML can change anytime and the agent could silently start returning nulls, fabricated ratings, or stale cached values. Defensive contract: if the scrape returns null or fails schema validation, the rating field renders as "rating unavailable" rather than fabricating. Adjacent sourcing decision: spokesperson asked for Morningstar specifically; Morningstar India has no free API; for the lean MVP we ship Value Research at runtime with a footnote naming the source, and log Morningstar-licensed-data-feed as a per-deployment client-cost item (clients pay for the feed at deployment; data licensing is a contract-level switch, not an architecture change). Codebase needs a clean swap-point between scrape source and licensed feed; that swap-point is a future workstream concern, not lean MVP. | Medium | v14 new, surfaced via T-5.15 provisional task | TBD |
| D12 | International ETF (Vanguard S&P 500 et al.) and opaque AIF holdings lack cap-classification metadata in the current snapshot, so T-5.14's client-specific weighted equity-only benchmark cannot include them in the cap-weight aggregation. v14 design choice: surface the gap honestly as a footnote on the rendered output ("X% of equity included in benchmark calculation") rather than enriching the snapshot. The primary explicitly stated preference: agent-runtime accommodations are okay; re-touching the snapshot data is not. Enrichment is the cleaner long-term path; deferred until either a production-data-onboarding workstream opens or a client demands cap-inclusive international benchmarking. UPDATE 2026-06-02 (T-5.14): the coverage footnote is implemented (`buildCoverageFootnote` in `risk-reward-stats.ts`) and ships in the re-fired cases' `risk_reward_stats` (the capability ships the data + footnote text; the rendered surface is T-5.09, WA9). The enrichment path for cap-inclusive international remains the deferred long-term option. | Medium | v14 new, surfaced via T-5.14 provisional task | TBD |
| D13 | Synthetic snapshot `macro.data_snapshot.dimensions` is only partially time-stepped across the t0..t8 suite: 11 of 24 macro indicators evolve (PMI manufacturing and services, GDP, CPI headline / core / food, repo rate 5.25 to 4.75 at t3, RBI stance Neutral to Accommodative at t3, US Fed funds, USD/INR, and the Dimension 5 news flow) while 13 are frozen at their t0 values across all nine snapshots: Non-Food Credit Growth (14.3%), Capacity Utilization (75.6%), IMD Monsoon, Real Interest Rate (0.0315), 10Y Govt Bond Yield (6.99%), WALR (8.28%), the three Government Fiscal Policy rows (still FY26 / FY27 labelled at t8 = Q2 2028), India-US Rate Differential (still labelled "5.25% - 3.625%" after the repo cut), Brent Crude ($118.95 "close Apr 29"), FII Net Flows (April), DII Net Flows (April). Several frozen rows are internally inconsistent with the rows that moved: the 10Y yield, real rate, and India-US differential should have shifted when the repo cut at t3, and the news flow is itself stale at t7 (normalisation) and t8 (smallcap rally), both still carrying the t6 RIL-idiosyncratic text. Impact on house view (T-5.04): the per-snapshot narrative arc cannot rest on the frozen indicators; the moving signal for each beat lives in the evolving indicators plus the index series (e.g. nifty_bank_tri t4 1097 to t5 978 for the bank shock) and FX, not in the macro block alone. Same class as the cluster-3 synthetic-data items (D8) but distinct from D8's four named items; surfaced by the T-5.04 house view Stage-1 audit (2026-06-02). | Medium | House view T-5.04 Stage-1 audit | Snapshot macro time-stepping pass (with the cluster-3 source-data cleanup / snapshot regeneration) |

