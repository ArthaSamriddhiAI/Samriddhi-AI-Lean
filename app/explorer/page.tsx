/* Explorer dashboard stub. The functional implementation lands in slice 5. */

export default function ExplorerPage() {
  return (
    <div className="mx-auto max-w-[var(--container-app)] px-7 py-12">
      <div className="eyebrow mb-2">Workspace</div>
      <h1>Explorer</h1>
      <p className="mt-4 max-w-prose text-ink-3">
        Model portfolio reference, snapshot inspection, and firm-level customisation surfaces
        land here in slice 5. For now the tab is visible to preserve visual parity with the wireframe.
      </p>
      <p className="mt-6 text-small text-ink-4 font-mono">Coming in slice 5.</p>
    </div>
  );
}
